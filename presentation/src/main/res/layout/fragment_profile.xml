<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/fragment_profile_layout_padding">

    <ImageView
        android:id="@+id/user_avatar"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_launcher_background"/>

    <TextView
        android:id="@+id/user_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/frgament_profile_user_name_text_margin_top"
        android:text="Sergey Pinkevich"
        android:textColor="@color/dark_grey"
        android:textSize="@dimen/frgament_profile_user_name_text_size"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/user_avatar"/>

    <TextView
        android:id="@+id/user_email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/frgament_profile_user_email_text_margin_top"
        android:text="sergeipinkevich163@gmail.com"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/user_name"/>

    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="@dimen/frgament_profile_divider_margin_top"
        android:background="@android:color/darker_gray"
        app:layout_constraintTop_toBottomOf="@id/user_email"/>

    <ImageView
        android:id="@+id/user_location_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_location"
        app:layout_constraintTop_toBottomOf="@id/divider"/>

    <TextView
        android:id="@+id/user_location_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Innopolis"
        app:layout_constraintBottom_toBottomOf="@id/user_location_icon"
        app:layout_constraintLeft_toRightOf="@id/user_location_icon"
        app:layout_constraintTop_toTopOf="@id/user_location_icon"/>

</android.support.constraint.ConstraintLayout>